const AdminRoute = ({ children }) => {
  //Revise logic code to check for admin role
  //   const isAdmin = localStorage.getItem('role') === 'admin';
  //   return isAdmin ? children : <Navigate to="/unauthorized" replace />;
};

export default AdminRoute;
